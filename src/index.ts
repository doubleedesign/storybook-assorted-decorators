import { withMaxWidth, maxWidthControls } from "./with-max-width/with-max-width";
import { withFocusTrap } from "./with-focus-trap/with-focus-trap";

export {
	withMaxWidth,
	maxWidthControls,
	withFocusTrap
};